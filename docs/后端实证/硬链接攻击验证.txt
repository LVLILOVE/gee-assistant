================================================================================
硬链接攻击复现验证（修复后）
================================================================================

[硬链接 .env 后读取（原穿透点）]
  BLOCKED_AT_LINK: 沙箱安全策略禁止创建链接（os.link）：链接可绕过路径检查读取敏感文件

[软链接 .env 后读取]
  BLOCKED_AT_SYMLINK: 沙箱安全策略禁止创建链接（os.symlink）：链接可绕过路径检查读取敏感文件

[硬链接 app.db 后读取]
  BLOCKED_AT_LINK

结果：泄露 0 / 3   ✅ 全部拦住