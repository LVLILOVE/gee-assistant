# 产品剩余任务清单（实测复核版）

复核时间：2026-09-20 15:10 (GMT+8)
复核方式：**不读文档、不凭记忆**，直接查生产库 `data/app.db` + 代码 + 文件系统
（依据：项目既有教训「验证失败时先怀疑测试台，再怀疑被测代码」的反向同理——
「盘点完成度时先怀疑自己的记忆，再相信文档」）

---

## 零、先说一个重要更正

我在上一轮汇总里说过「反馈数据仅 1 条自测」「users=1」。
**实测复核后必须更正两处**：

| 我上一轮说的 | 实测事实 |
|---|---|
| 「真实用户 UAT 仍为 0」 | **已有第 1 个真实用户注册**：`lvzhi`（id=5，2026-09-20 14:03:41） |
| 「反馈仅 1 条自测」 | 这条 `up` 反馈**发生在 lvzhi 注册后 39 分钟**（10:42:37）——当时库中确实只有 admin，所以仍是自测；但"0 真实用户"的结论已过期 |

更正原因：我上一轮读的是缓存的备忘文件，没重新查库。
**这正是本项目反复踩过的坑（读缓存当读现场），我自己又犯了一次。**

---

## 一、实测得到的关键数字

### 1.1 任务与失败率

```
总数 66   成功 61   失败 5   → 全期失败率 7.6%

按日期切片：
  09-15  总  5  失败 0   0.0%
  09-16  总 17  失败 1   5.9%  ★
  09-17  总 29  失败 2   6.9%  ★
  09-20  总 15  失败 2  13.3%  ★   ← 最新一天最高
```

### 1.2 5 条失败的真实原因（全部解密后逐条读日志）

| 任务 | 类型 | 区域 | 日期 | 真实原因 |
|---|---|---|---|---|
| 1aaa4c88f3ea | water | 洞庭湖 | 09-20 | `AttributeError: 'SandboxResult' object has no attribute 'stats'` （**代码缺陷**，非模型问题） |
| d14d708366bc | change_detection | 深圳市 | 09-20 | `EEException: Image.gt: If one image has no bands...` ×3（空波段） |
| 140f1a9f66c4 | classification | 长江三角洲 | 09-17 | `AttributeError: 'Image' object has no attribute 'mode'` ×3 |
| 0645a46f8523 | change_detection | 鄱阳湖 | 09-17 | `KeyError: 'ndvi'` ×3 |
| 59ac516a3a60 | ndvi | 太湖流域 | 09-16 | `User memory limit exceeded` → 调试后转 timeout ×2 |

### 1.3 三个"以为没修、其实已修"（核查代码后确认）

| 原失败原因 | 状态 | 证据 |
|---|---|---|
| `SandboxResult.stats` 缺失 | ✅ **已修** | `app/execution/sandbox.py:54` 已有 `stats` 字段，注释写明「2026-09-20 端到端实测抓到」 |
| `KeyError: 'ndvi'` | ✅ **已加定向提示** | `app/debugger.py:170` `_KEYERROR_RE`，提示"几乎肯定是取值语法，不是键名拼错" |
| `Image' has no attribute 'mode'` | ✅ **已加定向提示** | `app/debugger.py:131` `_EMPTY_BAND_RE`，注释直指「0 波段影像运算报错」 |

→ **这三条属于"已经闭环但清单里还挂着"的历史项，不该再算作待办。**

---

## 二、真正还没做的（按优先级）

### P0 · 必须在答辩前完成（1 项）

**① 全流程录屏兜底**
- 实测：`find` 全工程 **0 个 mp4 / 0 个录屏文件**
- 为什么是 P0：cpolar 免费版**重启换域名、隧道会自己死**（项目已实证发生过多次）。
  答辩现场网络一出问题，没有录屏就等于没有演示。
- 归谁：**用户侧动作**（需要真人操作 + 麦克风讲解），我无法代做。
- 建议：3–5 分钟，跑一条完整链路（登录 → 输入自然语言 → 生成代码 → 出图出表 → 结论卡片 → 点👍）

### P1 · 影响"产品可信度"，建议尽快（3 项）

**② 参数回显缺失 —— ✅ 已完成（2026-09-20 16:05）**

原缺口：`GET /api/tasks/{tid}` 未返回 `cloud_threshold`，导致
「重新运行」永远退回默认 20、报告导出显示「云量阈值：%」、用户无法核对参数。

**已修**：
- 后端 `get_task()` 补字段；`store.list()` 的 SELECT 补
  `start_date / end_date / cloud_threshold`
- 前端任务详情区新增参数回显行（区域 · 时间范围 · 云量）
- 顺带修正该列的浮点渲染（`37.0%` → `37%`），报告与前端口径统一
- 取不到时写「未记录」而非留空（空值看着像故障）

**验证**：新建契约测试 `tools/test_task_echo.py`，**22/22 通过**；
已接入 `regress_all.sh`；全套回归 **371/371**。
详见 `产品评估报告.md` §十二。

**③ UAT —— 只有 1 个真实用户，0 份真实反馈**

- 实测：`lvzhi` 注册于 09-20 14:03，**注册后未提交任何任务**（该账号任务数 = 0）
- 唯一那条 `up` 反馈（10:42:37）发生在他注册之前 → **仍是自测**
- 这是**最大的产品风险**，且**只能由用户侧解决**：找 2–3 个同学/同事各跑 1 条真实任务并反馈。
- **已准备好物料**：`docs/UAT引导.md` —— 含
  ① 可直接复制发给测试者的话术（已写明"真的在调 GEE 算影像"防误判卡死、
  明确要 👍/👎、明确鼓励报错）；
  ② 收集结果与判读的完整命令；
  ③ 公开前 4 步检查清单（含"发链接前必须验公网地址真的通"）；
  ④ 判定标准：≥3 用户 + ≥3 任务 + ≥3 反馈才算有数据，**1 条不能当结论**。

**④ 失败率监控 —— ✅ 已完成（2026-09-20 16:05）**

- 原状态：日失败率 09-15→09-20 为 `0% / 5.9% / 6.9% / 13.3%`，**无任何机制主动报警**
- **已修**：`demo_doctor.py` 新增 **L9 失败率趋势**
  - 切「近 24h」与「此前」两段分别算失败率
  - **先卡样本量再卡比率**（`n<5` 只 WARN）—— 防小样本天天误报
  - 分级：近期 ≥30% → FAIL；较历史翻倍且 ≥15% → WARN
  - 判定逻辑抽成纯函数 `_failure_rate_state()` 以便自检直接喂数据
- **验证**：`demo_doctor --self-test` 从 17 项扩到 **40/40**，新增 7 项覆盖切段与比率边界
- 对生产库实测输出：`全期 66 条失败 7.6%；近 24h 15 条 13.3%（历史 5.9%）→ OK`
  （13.3% 未报警是**正确**的：15 条样本未达 30% 红线）

### P2 · 产品化演进（4 项，原样保留）

**⑤ cpolar 替换 / 自动化**
免费版：重启换子域名、隧道自死、传输不稳。答辩用公网链接本身有风险。
可选替代：cloudflared / frp + 自有 VPS / 直接用局域网 IP。

**⑥ 成本看板**
DeepSeek 调用量与花费目前不可见。任务表已存 `attempts`，
结合 token 估算可以做出「本日/本周调用次数 + 预估成本」。

**⑦ 报告导出升级（PDF / Word）**
现在是 Markdown / HTML。答辩如果要求正式文档格式，需要一个转换步骤。

**⑧ MCP Server 化**
把遥感分析能力暴露成 MCP 工具，供其他 Agent 调用。属于扩展性演进，非验收必需。

---

## 三、已被我主动关闭的（不用再管）

- ✅ 业务数据加密（region/code/result/logs/feedback_note，254/254 = 100%）
- ✅ `DEEPSEEK_API_KEY` 轮换（用户已换新 key）
- ✅ `stats` 契约 bug（`SandboxResult.stats` 已补）
- ✅ 沙箱 3 类高危漏洞（写入敞开 / 破坏性操作 / **硬链接泄露密钥**）
- ✅ 沙箱回归守卫 76 项 + 全套 354 项
- ✅ 3 项任务书交付物（架构设计图 / 交互原型图 / 提示词模板库）
- ✅ 结论摘要 + 反馈通道
- ✅ 14 页 PPT

---

## 四、必须说清的两条限制（不是"待办"，是"已定性的现实"）

**① 沙箱非容器隔离（架构级）**
沙箱与主服务**同机同用户**。已用能力级封禁（读黑名单 / 写白名单 / 破坏性 / 链接 /
进程 / 网络）把常见逃逸路径全部封死，并留下 76 项守卫。
但**真正的强隔离需要 OS 级手段**（受限账户 / 容器 / Job Object），
这不是"再补几个 guard"能解决的。答辩若被问及，应如实说明这是已知边界。

**② `.env` 的 `ALLOW_REGISTRATION=true`**
线上仍开放注册。答辩前建议评估是否收紧（加了真实用户后再关，否则 lvzhi 无法二次登录）。

---

## 五、一句话结论

**功能层面已经完整，剩下的是"可信度"和"演示风险"两类问题。**

**本轮（2026-09-20 16:05）已由我完成的：**
- ✅ 参数回显缺口（详情 + 列表 + 报告，含浮点格式化）
- ✅ 契约守卫 `test_task_echo.py` 22 项，已接入回归
- ✅ 失败率监控 `demo_doctor` L9，自检 17 → **40 项**
- ✅ UAT 物料 `docs/UAT引导.md`（话术 + 判读命令 + 公开前检查清单 + 判定标准）
- ✅ 前端重建上线（`index-rgIG4N-3.js`，懒加载未破）
- ✅ 全套回归 **371/371**（原 354）

**必须由你做的（我无法代做）：**
1. **全流程录屏**（P0）—— 需要真人操作与讲解
2. **找 2–3 个真实用户跑 UAT**（P1）—— 物料已备好，直接发话术即可
3. **重启 cpolar 隧道** —— 实测 16:02 时进程不存在、4040 未监听，**隧道已停**

**剩下可选的（P2）**：cpolar 替换 / 成本看板 / 报告导出 PDF·Word / MCP Server 化。

失败率 7.6% 里，5 条失败有 3 类根因已被定向修复（`stats` / `KeyError` / 空波段），
说明**失败样本已经被消化过一轮**，不是放任状态；现在还有 L9 盯着趋势。
